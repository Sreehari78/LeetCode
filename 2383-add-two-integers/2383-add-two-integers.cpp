class Solution {
public:
    int sum(int num1, int num2) {
        std::ios_base::sync_with_stdio(false);
        std::cin.tie(nullptr);
        std::cout.tie(nullptr);
        return num1 + num2;
    }
};